from controller.data_controller import *

class Voivodeship:
    voivodeships = []

    def __init__(self, name, id_, voivodeship_id, type_name):
        self.name = name
        self.voivodeship_number = int(voivodeship_number)
        self.type_name = type_name
